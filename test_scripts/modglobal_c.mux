// Constants whose names begin with builtin-function prefixes (print, range,
// some, none, ok, err, read_line). These must survive a wildcard import; the
// builtin-name guard applies to functions, not constants.
const int err_threshold = 7
const int print_width = 80
const string some_label = "wildcard"
