// Regression for mux-compiler#289: a generic collection type used without its
// type arguments (here `list` with no <T>) must be a semantic error pointing at
// the user's code, not silently accepted.
class Container<T> {
    list items
}

auto c = Container<int>.new()
print("unreachable")
