// mux-compiler#289 review: a generic enum used with the wrong number of type
// arguments must be rejected (enum symbols now record their type parameters).
enum MyBox<T> { Of(T value), Empty }

class Container {
    MyBox<int, string> b
}

print("unreachable")
