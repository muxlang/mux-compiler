// Constructing an enum variant that violates its where clause panics.

enum Status {
    Unknown,
    Code(int value) where { value >= 100, value <= 599 }
}

auto bad = Status.Code(9999)
print("unreachable")
