// Should fail: an enum named after a built-in type does not get the structural
// comparison a user enum has, since codegen excludes those names
enum optional { A, B }
auto x = optional.A
auto y = optional.B
print((x == y).to_string())
